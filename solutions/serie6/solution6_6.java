

public class solution6_6 {
    public static void main(String[] args) {
        int[] valeurs = {4, 8, 2, 10};

        System.out.printf("(%d + %d) * 2 = %d\n",
                                        valeurs[0], 
                                        valeurs[3], 
                                        (valeurs[0] + valeurs[3]) * 2
                         );
    }
}
